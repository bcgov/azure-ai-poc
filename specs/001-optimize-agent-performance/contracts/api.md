# Contract: Public API

This feature must not change the externally visible API behavior.

## Endpoints

- No endpoints added/removed.
- No request/response schema changes.
- No new headers required.

## Behavior

- Responses must remain functionally equivalent to current behavior.
- Any caching is internal and must not leak across tenants/users.
